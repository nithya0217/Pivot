version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://postgres:PiVotN09123@db.tjfqvppsaqcfnrueccuv.supabase.co:5432/postgres
      - ENV=production
      - PORT=8000
